{% extends 'layout.twig' %}

{% block title 'Error' %}
{% block content %}
<!--------------Content--------------->
<section id="content">
    <div class="wrap-content">
        <div class="row block">
            <div id="main-content" class="col-2-3">
                <div class="wrap-col">
                    <article>
                        <div class="heading">
                            <h2> Error </h2>
                        </div>
                        <div class="content">
                            <p>  {{ mensaje|default('Página no encontrada.') }} </p>
                        </div>
                    </article>
                </div>
            </div>
            <!-- Aside -->
            {% include '_aside.twig' with { 'agenda': agenda, 'config': config, 'site': site, "home": "false" } %}
	</div>
    </div>
</section>
{% endblock %}